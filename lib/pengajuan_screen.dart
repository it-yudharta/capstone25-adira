import 'package:flutter/material.dart';

class PengajuanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Pengajuan")),
      body: Center(child: Text("Halaman Pengajuan")),
    );
  }
}
