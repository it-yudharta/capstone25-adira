import 'package:flutter/material.dart';

class GenerateQRPendaftaran extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Generate QR for Pendaftaran')),
      body: Center(
        child: Text('Page Kosong untuk Pendaftaran', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
